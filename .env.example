# ============================================================================
# .env.example - Variables d'Environnement pour Odyssée SaaS
# ============================================================================
# 
# INSTRUCTIONS :
# 1. Copiez ce fichier et renommez-le en ".env"
# 2. Remplissez les valeurs avec vos vraies clés
# 3. NE COMMITEZ JAMAIS le fichier .env sur GitHub
#
# ============================================================================

# ============================================================================
# SÉCURITÉ
# ============================================================================

# Clé secrète Flask (CHANGEZ EN PRODUCTION !)
# Générez-en une avec : python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-change-in-production-2025

# Clé maître de chiffrement (CRITIQUE - NE JAMAIS CHANGER EN PROD)
# Sert à chiffrer les clés API des agences stockées en BDD
# Générez-en une avec : python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
MASTER_ENCRYPTION_KEY=your-master-encryption-key-NEVER-CHANGE

# ============================================================================
# BASE DE DONNÉES
# ============================================================================

# SQLite en développement (par défaut)
DATABASE_URL=sqlite:///odyssee.db

# PostgreSQL en production (Railway)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# ============================================================================
# SUPER ADMIN (COMPTE INITIAL)
# ============================================================================

# Ces identifiants sont utilisés pour créer le compte super-admin
# lors de la première initialisation (commande: flask init-db)
SUPER_ADMIN_USERNAME=superadmin
SUPER_ADMIN_PASSWORD=ChangeMe2025!
SUPER_ADMIN_EMAIL=admin@odyssee-saas.com

# ============================================================================
# FLASK
# ============================================================================

# Environnement (development | production)
FLASK_ENV=development

# Mode debug (1 = activé, 0 = désactivé)
FLASK_DEBUG=1

# ============================================================================
# APIS GOOGLE (OPTIONNEL - Peut être configuré par agence)
# ============================================================================

# Google Places API Key (pour autocomplete hôtels, lieux)
# 🔒 IMPORTANT : Cette clé n'est JAMAIS exposée au client
# Elle reste côté serveur et est utilisée par les routes proxy /api/google/*
# Obtenez votre clé sur : https://console.cloud.google.com/
GOOGLE_PLACES_API_KEY=

# Google Gemini API Key (pour l'intelligence artificielle)
# Utilisée pour parser les prompts en langage naturel
# Obtenez votre clé sur : https://ai.google.dev/
GOOGLE_GEMINI_API_KEY=

# YouTube Data API Key (pour récupérer vidéos de destinations)
YOUTUBE_API_KEY=

# ============================================================================
# STRIPE (OPTIONNEL - Peut être configuré par agence)
# ============================================================================

# Clé API Stripe pour les paiements
# Obtenez vos clés sur : https://dashboard.stripe.com/apikeys
STRIPE_API_KEY=

# Secret Webhook Stripe
STRIPE_WEBHOOK_SECRET=

# ============================================================================
# MAIL (OPTIONNEL)
# ============================================================================

# Serveur SMTP pour l'envoi d'emails
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=
MAIL_PASSWORD=

# ============================================================================
# PRODUCTION (RAILWAY)
# ============================================================================

# URL publique du site (pour génération de liens)
SITE_PUBLIC_URL=https://odyssee-saas.up.railway.app

# CORS Origins (domaines autorisés, séparés par des virgules)
CORS_ORIGINS=*

# ============================================================================
# NOTES IMPORTANTES
# ============================================================================
#
# 🔐 SÉCURITÉ :
# - Ne jamais commiter ce fichier sur GitHub
# - Ajouter ".env" dans votre .gitignore (déjà fait)
# - Utiliser des clés différentes en dev et prod
# - Ne jamais exposer MASTER_ENCRYPTION_KEY (perte de toutes les données chiffrées)
#
# 🏢 MULTI-TENANT :
# - Les clés Google/Stripe peuvent être définies :
#   1. Globalement ici (toutes les agences)
#   2. Par agence (via l'interface super-admin)
# - La priorité est donnée à la clé de l'agence si elle existe
#
# 🚀 DÉPLOIEMENT RAILWAY :
# - Railway injecte automatiquement DATABASE_URL (PostgreSQL)
# - Définir toutes les variables dans l'interface Railway
# - Variables essentielles : SECRET_KEY, MASTER_ENCRYPTION_KEY
#
# ============================================================================